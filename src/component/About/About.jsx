import React from "react";
import NavBar from "../Navbar/NavBar";

const About = (props) => {
  // console.log(data);
  return (
    <div>Hey I'm About {props.name}</div>
  )
};

export default About;
