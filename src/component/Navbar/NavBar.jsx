import React from "react";
import About from "../About/About";
const NavBar = ({ name,check }) => {
  return (
    <div>
      navbar
      {/* <About data={name} /> */}
    </div>
  );
};

export default NavBar;
